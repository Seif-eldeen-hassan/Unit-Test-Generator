# Unit-Test-Generator
An AI-powered Python CLI tool that generates strict, deterministic, and executable unit tests for single functions, featuring AST-based code sanitization and prompt injection prevention.
